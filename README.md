# KinectDK
Repository for Microsoft Azure Kinect DK C++ wrapper class and utilities

Note: Currently must copy k4a.lib library to build folder, and copy k4a.dll, depthengine2.0.dll to run folder for Windows build and execute. 
